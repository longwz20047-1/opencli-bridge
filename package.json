{
  "name": "opencli-bridge",
  "version": "0.1.0",
  "description": "OpenCLI Bridge - connects local CLI tools to AgentStudio",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && electron .",
    "dev": "tsc && electron ."
  },
  "dependencies": {
    "cross-spawn": "^7.0.3",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/cross-spawn": "^6.0.6",
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.10",
    "electron": "^28.0.0",
    "typescript": "^5.3.0"
  }
}
